<UserControl
    x:Class="CustomComponents.CommentProfile.CommentProfile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CustomComponents.CommentProfile"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="100">

    <Grid>
        <Grid x:Name="ProfileImage">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Image Grid.Row="0" Grid.Column="0"
                   x:Name="ProfilePic" 
                   DataContext="{x:Bind viewModel.profilePic}"/>
            <TextBlock Grid.Row="1" Grid.Column="0"
                       x:Name="ProfileName" 
                       DataContext="{x:Bind viewModel.userProfile.Name}"/>
        </Grid>
    </Grid>
</UserControl>
